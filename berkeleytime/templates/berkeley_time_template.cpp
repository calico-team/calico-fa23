#include <iostream>

using namespace std;

/**
 * Return the berkeleytime value of A.
 * 
 * A: a non-negative integer
 */
int solve(int A) {
    // YOUR CODE HERE
    return -1;
}

int main() {
    int T;
    cin >> T;
    for (int i = 0; i < T; i++) {
        int A;
        cin >> A;
        cout << solve(A) << '\n';
    }
}
